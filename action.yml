name: "AI Algorithm Mentor"
description: "algorithm code review using LLM"
author: "choam2426"
inputs:
  GITHUB_TOKEN:
    description: "GitHub personal access token for repository access"
    required: true
  LLM_PROVIDER:
    description: "AI model provider to use for code review (openai, google, anthropic)"
    required: true
    default: "google"
  MODEL_NAME:
    description: "Model name to use for code review (e.g., gpt-5.4-mini, gemini-3.1-flash, claude-sonnet-4-6). If omitted, a default model is selected based on LLM_PROVIDER."
    required: false
  OPENAI_API_KEY:
    description: "OpenAI API key (required when LLM_PROVIDER is 'openai')"
    required: false
  GEMINI_API_KEY:
    description: "Google Gemini API key (required when LLM_PROVIDER is 'google')"
    required: false
  ANTHROPIC_API_KEY:
    description: "Anthropic Claude API key (required when LLM_PROVIDER is 'anthropic')"
    required: false
  REVIEW_LANGUAGE:
    description: "Language for code review comments (e.g., english, korean, german)"
    required: false
    default: "korean"

runs:
  using: "docker"
  image: "docker://choam2426/ai_algorithm_mentor:v6"
  env:
    GITHUB_TOKEN: ${{ inputs.GITHUB_TOKEN }}
    LLM_PROVIDER: ${{ inputs.LLM_PROVIDER }}
    MODEL_NAME: ${{ inputs.MODEL_NAME }}
    OPENAI_API_KEY: ${{ inputs.OPENAI_API_KEY }}
    GEMINI_API_KEY: ${{ inputs.GEMINI_API_KEY }}
    ANTHROPIC_API_KEY: ${{ inputs.ANTHROPIC_API_KEY }}
    REVIEW_LANGUAGE: ${{ inputs.REVIEW_LANGUAGE }}
branding:
  icon: "check-circle"
  color: "blue"
